{
  "name": "evxchange-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "heroku-postbuild": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "@craco/craco": "^7.1.0"
  },
  "engines": {
    "node": "22.x",
    "npm": "10.x"
  }
}
